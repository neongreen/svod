$(document).ready(function () {

    // put anchors
    anchors.options = {
        placement: 'left',
        visible: 'always',
        class: 'anchor'
    };
    // anchors.options.visible = 'always';
    anchors.add('h1, h2, h3');

});

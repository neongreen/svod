$(#{toJSONId buttonId}).popover({
    trigger: 'hover',
    content: 'Войдите или зарегистрируйтесь чтобы отмечать публикации.'
});

$(#{toJSONId buttonId}).click(function(event) {
    window.location.href = '@{LoginR}';
});
